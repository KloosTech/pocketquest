*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings

# v1: the old PoC, kept locally as reference only, never tracked here
/v1/

# runtime artifacts of :app:run's demo pipeline, not source
/app/pocketquest-demo.db*

assets/normalized/

# Design reference material only (Geomorph crosshatch scans) — not consumed by any build step,
# the wall hatch is drawn procedurally, not extracted from these. Kept locally for design reference.
assets/*.png
assets/geomorph collection part 1/

# Claude Code local session state, not project source
/.claude/
